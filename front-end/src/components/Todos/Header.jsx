import React from 'react';

const Header = () => {
    return (
        <header>
            <h1>To-Dos React Application</h1>
            <p>This is an Express-React application for studying front and back-end in React applications!</p>
        </header>
    );
};

export default Header;
